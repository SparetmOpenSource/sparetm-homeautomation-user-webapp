import './Button.css';
import { Link } from 'react-router-dom';

const ButtonLink = (props: any) => {
    return (
        <Link to={props?.to}>

        </Link>
    );
};

export default ButtonLink;
