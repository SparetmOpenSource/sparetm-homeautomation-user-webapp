import { Link } from 'react-router-dom';
import './button.css';

const ButtonLink = (props: any) => {
    return (
        <Link to={props?.to}>

        </Link>
    );
};

export default ButtonLink;
